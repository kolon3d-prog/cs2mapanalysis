# Bugfix: {{Краткое имя дефекта}}

## Introduction

{{Что сломано, в каких условиях проявляется, чего это стоит.}}

## Current Behavior (Defect)

<!-- Без SHALL: это констатация, а не требование. Иначе дефект и ожидаемое поведение неразличимы. -->

WHEN {{условие и вход, на котором ломается}}
THEN the system {{ошибочное поведение}}

WHEN {{второй сценарий, если есть}}
THEN the system {{ошибочное поведение}}

## Expected Behavior (Correct)

<!-- Здесь SHALL: обычный критерий приёмки. -->

WHEN {{условие и вход}}
THEN the {{Система}} SHALL {{правильное поведение}}

WHEN {{граничный случай}}
THEN the {{Система}} SHALL {{правильное поведение}}

## Unchanged Behavior (Regression Prevention)

<!-- SHALL CONTINUE TO, дословно. Секция не должна быть пустой:
     пустая означает, что рядом с багом нет работающего поведения — или о нём не подумали.
     Каждая строка обязана быть проверяемой: "работает как раньше" — не свойство. -->

WHEN {{условие, где всё работает}}
THEN the {{Система}} SHALL CONTINUE TO {{прежнее поведение}}

WHEN {{соседняя функциональность, которую легко сломать фиксом}}
THEN the {{Система}} SHALL CONTINUE TO {{прежнее поведение}}

<!-- ------------------------------------------------------------------ -->

## Материал для design.md (root cause и свойства)

<!-- Эта часть переносится в design.md, здесь оставлена как рабочий чеклист фазы анализа. -->

### Bug condition (C)

{{Предикат над входом и состоянием, отделяющий сломанные входы от рабочих. Именно над входом, не над кодом.}}

### Postcondition (P)

{{Что значит «исправлено» — наблюдаемо.}}

### Группы свойств

| Группа | Свойство | Ожидание на нефиксированном коде |
|---|---|---|
| Баг существует | на входах внутри `C` результат не соответствует `P` | падает |
| Фикс работает | `C ⇒ P` | падает, затем зеленеет после патча |
| Сохранность | `¬C ⇒ поведение не изменилось` | **проходит** — это baseline |

<!-- Порядок задач жёсткий:
     1. bug-condition тесты (падают на нефиксированном коде);
     2. preservation-тесты (проходят на нефиксированном коде — он и есть эталон сохранности);
     3. патч.

     После патча: bug-condition краснеет → зеленеет; preservation остаётся зелёной.
     Bug-condition не позеленел — гипотеза о root cause неверна, возврат к исследованию.
     Preservation сломалась — у фикса побочные эффекты, патч сужается. -->
