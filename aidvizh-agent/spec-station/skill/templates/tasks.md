# Implementation Plan

<!-- Гейт ревью пройден: покрытие требований и исполнимость проверены. -->

- [ ] 1. {{Основание: окружение, тестовая инфраструктура, схема данных, конфигурация}}
  - {{Что именно делается}}
  - _Requirements: {{1.1}}_

- [ ] 2. {{Основная часть фичи}}
- [ ] 2.1 (P) {{Подзадача}}
  - {{Деталь}}
  - _Requirements: {{2.1, 3.3}}_
  - _Boundary: {{КомпонентИзДизайна}}_

- [ ] 2.2 (P) {{Подзадача без пересечения по компонентам}}
  - {{Деталь}}
  - _Requirements: {{2.4}}_
  - _Boundary: {{ДругойКомпонент}}_

- [ ] 3. {{Соединение частей}}
- [ ] 3.1 {{Интеграция компонентов}}
  - {{Деталь}}
  - _Depends: {{2.1, 2.2}}_
  - _Requirements: {{3.1}}_

- [ ] 4. {{Проверки}}
- [ ]* 4.1 {{Отложенные тесты: только тестовое покрытие может быть опциональным}}
  - {{Какой критерий приёмки закрывает}}
  - _Requirements: {{1.2}}_

<!-- Формат:
     - заголовок ровно "# Implementation Plan";
     - только два уровня: "1." и "1.1", третий уровень запрещён;
     - " (P)" ставится сразу после номера, вне чекбокса, только когда выполнены все пять условий
       параллельности и области _Boundary:_ не пересекаются;
     - "_Requirements:" — только числовые ID, через запятую, без описаний;
     - "_Boundary:" — обязательно для (P), иначе опускается;
     - "_Depends:" — только для неочевидных зависимостей между разными группами задач;
     - "- [ ]*" — опциональная задача; массовый прогон её пропускает;
     - статусы обновляются по ходу: "- [x]" готово, "- [F]" не удалось (причина — в деталях).

     Запрещено класть сюда: деплой, документацию, обучение пользователей, сбор фидбэка, маркетинг,
     орг-изменения, ручной прогон приложения, сбор метрик производительности,
     любую задачу, которую нельзя выполнить написанием, правкой или тестированием кода.

     Проверка перед записью:
     - каждый ID требований встречается хотя бы в одной задаче;
     - каждый компонент и контракт из дизайна представлен задачей;
     - каждая подзадача проверяема и укладывается в 1–3 часа;
     - маркеры (P), _Boundary:_ и _Depends:_ не противоречат друг другу. -->
