---
type: Post
title: Rejudge — независимая проверка кода тремя агентами и ИИ-судьёй
description: 'Инструмент для независимой проверки кода: три разных агента отдельно
  исследуют код в изолированных контекстах, ИИ-судья сравнивает отчёты, задаёт уточняющие
  вопросы и выдаёт один готовый отчёт с ошибками, рисками и уязвимостями.'
date: 2026-08-11
tags:
- ai
- agents
- code-review
- testing
source: https://github.com/syabro/rejudge
status: stable
generated:
  by: build-agent/aggg2.0
  at: 2026-08-11 12:23:40+00:00
permalink: wiki-vibecoding/ai/rejudge
---

# Rejudge — независимая проверка кода тремя агентами и ИИ-судьёй

Оригинальный пост:

⚖️ Вышел классный инструмент Rejudge для независимой проверки кода — три разных агента отдельно исследуют код, а судья сравнивает результаты и выдаёт один готовый отчёт.

Как работает:

1. Каждая модель получает один и тот же запрос и разбирает его в изолированном контексте: ни одна не видит, что делают другие.
2. ИИ-судья проверяет все три отчёта и задаёт уточняющие вопросы там, где независимые проверки расходятся.
3. Судья отдаёт один готовый отчёт с ошибками, рисками и уязвимостями в коде.

Один агент хорошо, а связка агентов и судьи лучше — тут (https://github.com/syabro/rejudge).

@notboring_tech

## Контекст

- Пост готовый, ссылка не проверялась (по указанию автора).
- Паттерн: мульти-агентная проверка с изоляцией — N моделей анализируют код независимо (не видят друг друга), затем ИИ-судья сводит отчёты, проясняет расхождения и выдаёт единый отчёт (ошибки, риски, уязвимости).
- Отличие от простого ensemble: судья активно уточняет расхождения между независимыми проверками, а не просто усредняет.

## Вывод

Rejudge — практичная реализация «судейского» паттерна для code review: независимые проверки снижают эффект общей слепой зоны одной модели (разные модели ошибаются по-разному), а судья собирает консенсус. Близкая идея — мульти-агентная триангуляция (сравнение независимых выводов), которую мы используем и в ресёрче. Полезно как дешёвая альтернатива/дополнение классическому ревью.