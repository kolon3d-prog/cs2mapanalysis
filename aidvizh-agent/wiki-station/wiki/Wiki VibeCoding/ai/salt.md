---
type: Post
title: SALT — сжатие длинного контекста лексическим деревом тем
description: 'Salience-Aware Lexical Trie: сжатие длинного контекста для LLM; вместо
  выбора релевантных предложений строит лексическое дерево по ключевым темам документа;
  распределяет бюджет токенов между темами, предотвращает «коллапс тем» (theme collapse);
  работает с KV-cache.'
date: 2026-08-11
tags:
- ai
- llm
- context
- compression
- research
source: https://github.com/oteomamo/SALT
status: stable
generated:
  by: build-agent/aggg2.0
  at: 2026-08-11 13:12:35+00:00
permalink: wiki-vibecoding/ai/salt
---

# SALT — сжатие длинного контекста лексическим деревом тем

Оригинальный пост:

Соль для ллм:

Представлен SALT (https://github.com/oteomamo/SALT) (Salience-Aware Lexical Trie) — инструмент для эффективного сжатия длинного контекста в LLM.

Вместо простого выбора наиболее релевантных предложений, SALT строит лексическое дерево (trie) на основе ключевых тем документа. Это позволяет эффективно распределять бюджет токенов между различными темами и предотвращает «коллапс тем» (theme collapse), сохраняя важные детали в длинных диалогах.

Технология предлагает новый подход к управлению контекстом (KV-cache), позволяя эффективно работать с длинными документами при ограниченном бюджете токенов, сохраняя семантическое разнообразие.

#полезнаяхуйня

## Контекст

- Пост готовый, ссылка не проверялась (по указанию автора).
- Что: SALT (Salience-Aware Lexical Trie): сжатие длинного контекста через лексическое дерево тем; распределение бюджета токенов между темами; против «коллапса тем» (потери деталей второстепенных тем); для KV-cache.

## Вывод

Проблема, которую решает: при сжатии длинного контекста часто теряются второстепенные темы (все токены уходят главной). SALT — «сжатие с сохранением разнообразия тем» (ср. Headroom — сжатие, PageIndex — дерево). Для наших длинных сессий/баз — интересный подход.