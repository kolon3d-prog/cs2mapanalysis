---
type: Post
title: 'waku-agent — локальный ИИ-ассистент: цикл ~95 строк Python'
description: 'ИИ-ассистент от Shen Sean Chen (открытый исходник): Agent Harness и
  Loop Engineering на реальном коде; цикл: сообщение → память → агент → тулы → трассировка
  → оценка → память → навыки; SQLite; детерминированные и LLM-as-a-judge оценки; Telegram-шлюз,
  голос, Apple Calendar.'
date: 2026-08-11
tags:
- ai
- agents
- harness
- memory
- python
source: https://github.com/ShenSeanChen/waku-agent
status: stable
generated:
  by: build-agent/aggg2.0
  at: 2026-08-11 12:54:39+00:00
permalink: wiki-vibecoding/ai/waku-agent
---

# waku-agent — локальный ИИ-ассистент: цикл ~95 строк Python

Оригинальный пост:

Shen Sean Chen открыл (https://github.com/ShenSeanChen/waku-agent) исходный код своего локального ИИ-ассистента

Проект демонстрирует работу Agent Harness и Loop Engineering на примере реального кода. Ассистент полностью запускается на ноутбуке пользователя, а его основной цикл занимает около 95 строк на Python.

Вся система представляет собой один цикл:
сообщение → поиск в памяти → запуск агента → вызов инструментов → трассировка → оценка → сохранение в память → развитие навыков

Память хранится в одном файле SQLite. В проект также встроены детерминированные и LLM-as-a-judge оценки с проверкой перед релизом, Telegram-шлюз, голосовая активация и инструменты для работы с Apple Calendar.

В демонстрации (https://youtu.be/rvRyBhILrls?si=UTgJQ69rjuW9cOla) ассистент добавляет в календарь четвертьфинал чемпионата мира, запоминает друзей пользователя и отвечает через Telegram.

## Контекст

- Пост готовый, ссылки не проверялись (по указанию автора).
- Что: локальный ИИ-ассистент с открытым кодом: Agent Harness + Loop Engineering; основной цикл ~95 строк Python.
- Цикл из поста: сообщение → поиск в памяти → запуск агента → вызов инструментов → трассировка → оценка → сохранение в память → развитие навыков.
- Компоненты: SQLite-память (один файл), детерминированные + LLM-as-a-judge оценки (проверка перед релизом), Telegram-шлюз, голосовая активация, Apple Calendar.
- Демо: добавление события в календарь, запоминание друзей, ответы через Telegram.

## Вывод

Эталонный минимальный «харнес» агента (ср. наш CYCLE и Prime Agent): цикл как явная архитектура + память + самооценка + навыки. LLM-as-a-judge перед релизом — наш fable-judge. 95 строк ядра — наглядный урок: сложность не в цикле, а в инструментах и памяти.