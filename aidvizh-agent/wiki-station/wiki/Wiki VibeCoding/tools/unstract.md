---
type: Post
title: Unstract — LLM-экстрактор документов в структурированный JSON
description: 'Извлекает JSON из PDF, сканов и картинок (инвойсы, KYC, выписки, декларации)
  без regex и шаблонов: схемы парсинга через обычные промпты (Prompt Studio); REST
  API / ETL; интеграция с агентами через MCP; выгрузка в S3, GCS, Snowflake, BigQuery,
  Postgres; любые провайдеры.'
date: 2026-08-11
tags:
- tools
- documents
- parsing
- llm
- etl
source: https://github.com/Zipstack/unstract
status: stable
generated:
  by: build-agent/aggg2.0
  at: 2026-08-11 12:54:39+00:00
permalink: wiki-vibecoding/tools/unstract
---

# Unstract — LLM-экстрактор документов в структурированный JSON

Оригинальный пост:

Ему можно скормить:
• PDF-файл
• Инвойс (счет на оплату)
• KYC-анкету
• Банковскую выписку
• Налоговую декларацию

А на выходе вы получите чистый JSON, полностью готовый к загрузке в вашу базу данных.

Забудьте про:
❌ Бесконечные регулярки (regex)
❌ Шаблоны под каждого отдельного поставщика
❌ Обучение моделей под каждый новый формат

Вам больше не придется писать уникальный экстрактор под каждый тип документа.

Просто опишите текстом на обычном языке, какие данные вам нужны.

Unstract сам подключит LLM и вытащит нужные поля, даже если структура документа изменится.

Что он умеет:
→ Извлекать структурированный JSON из PDF, сканов и картинок.
→ Создавать схемы парсинга обычными промптами на английском через Prompt Studio.
→ Разворачиваться как REST API или ETL-пайплайн за считанные минуты.
→ Интегрироваться с Claude и другими агентами через протокол MCP.
→ Загружать данные в S3, GCS, Snowflake, BigQuery, Postgres и другие базы.
→ Работать на базе OpenAI, Anthropic, Bedrock, Gemini, Mistral, Ollama и других провайдеров.

https://github.com/Zipstack/unstract

## Контекст

- Пост готовый, ссылка не проверялась (по указанию автора).
- Что: LLM-экстракция структурированных данных из документов (PDF, сканы, картинки): инвойсы, KYC, выписки, декларации → чистый JSON.
- Заменяет: regex, шаблоны под поставщиков, обучение моделей под форматы; схема парсинга — обычный промпт (Prompt Studio).
- Возможности из поста: REST API / ETL; MCP-интеграция с агентами (Claude и др.); выгрузка в S3/GCS/Snowflake/BigQuery/Postgres; провайдеры: OpenAI/Anthropic/Bedrock/Gemini/Mistral/Ollama.

## Вывод

«Промпт вместо парсера» для документов (ср. anydoc/pdf-inspector — те про Markdown, Unstract — про структурированные поля). Для автоматизации финансовых/HR-процессов — сильный инструмент: устойчив к изменению структуры документов. MCP-интеграция — стандарт экосистемы.