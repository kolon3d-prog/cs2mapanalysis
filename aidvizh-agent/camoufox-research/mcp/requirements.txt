# Зависимости MCP-сервера веб-ресёрча Camoufox.
# Установка:  pip install -r mcp/requirements.txt
# Браузер (один раз): python -m camoufox fetch
#
# Версии запинены по реально работающему venv (проверено 08.2026) —
# без пинов pip дрейфует к новым версиям и ломает совместимость.
# ПОЛНЫЙ список = pyproject.toml [dependencies] (одна правда);
# здесь — то же самое для запуска mcp/server.py из исходников.
# uv.lock в корне РЕПО покрывает ВСЕ эти зависимости (включая mcp/):
# отдельный mcp/uv.lock = дублирование (DRY), НЕ создавать —
# обновление зависимостей только через корневой uv.lock.
mcp==2.1.1
camoufox==0.5.5
# опционально: чистый текст статьи (article_only) — без неё fallback на body
trafilatura>=2.0
pypdf>=4.0
python-docx>=1.1
openpyxl>=3.1
